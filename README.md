# subagent_manager
A repo when plugged in with any LLM, can handle a concurrent call of subagents. This repo enables LLM's tool calling ability, and makes more grounded, focused (no lost-in-the-middle), short context responses.
